import React from 'react';

const Projects = () => {
    return (
        <section id="projects" className="section">
            <div className="container">
                <h2 className="section-title">My Projects</h2>
                {/* Ajoutez une liste de projets ici */}
            </div>
        </section>
    );
};

export default Projects;